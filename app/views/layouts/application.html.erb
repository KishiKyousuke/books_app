<!DOCTYPE html>
<html>
  <head>
    <title>BooksApp</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <p>
      <% if user_signed_in? %>
        現在<strong><%= current_user.email %></strong>のアカウントでログインしています
        <%= link_to t('devise.shared.links.edit_profile'), edit_user_registration_path %> |
        <%= link_to t('devise.shared.links.sign_out'), destroy_user_session_path, method: :delete %>
      <% else %>
        <%= link_to t('devise.shared.links.sign_up'), new_user_registration_path %> |
        <%= link_to t('devise.shared.links.sign_in'), new_user_session_path %>
      <% end %>
    </p>
    <% if notice %>
      <p id="notice"><%= notice %></p>
    <% end %>
    <% if alert %>
      <p id="alert"><%= alert %></p>
    <% end %>
    <%= yield %>
  </body>
</html>
